/*eslint-disable*/

import Recipes from '../models';

class HandleAllRecipeRoutes {
    // API route handler that allows a user to add a recipe 
    addRecipe(request, response) {

    }
}

export default HandleAllRecipeRoutes;